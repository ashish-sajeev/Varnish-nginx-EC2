server {
  listen {{ nginx_port }};
  
  root /var/www/{{ domain_name }};
  index index.html;
  server_name {{ domain_name }} www.{{ domain_name }};
  location / {
    try_files $uri $uri/ =404;
  }
}
